<UserControl x:Class="POS_display.wpf.View.ItemReturnReport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS_display.wpf.View"
             xmlns:wpf="clr-namespace:POS_display.wpf"
             xmlns:POS_display="clr-namespace:POS_display"
             mc:Ignorable="d" 
             Height="900" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Content="Spausdinti" Height="40" HorizontalAlignment="Left" Command="{Binding PerformPrintCommand}" Margin="5,5,0,0"  
                Name="PrintDocumentButton" IsEnabled="{Binding IsPrintEnabled}" VerticalAlignment="Top" Width="134" />
        <FlowDocumentScrollViewer x:Name="FlowDocumentView" Grid.Row="1" Document="{Binding ReportDocument}">
        </FlowDocumentScrollViewer>
        <Button Content="Uždaryti" Height="40" HorizontalAlignment="Right" Command="{Binding CloseCommand}" Margin="0,0,5,5"  
            x:Name="CloseButton" VerticalAlignment="Center" Width="134" />
    </Grid>

</UserControl>
