<UserControl x:Class="POS_display.wpf.View.Payment"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS_display.wpf.View"
             xmlns:wpf="clr-namespace:POS_display.wpf"
             Loaded="UserControl_Loaded"
             mc:Ignorable="d" 
             Height="540" Width="760">
    <UserControl.Resources>
        <ResourceDictionary>
            <FrameworkElement x:Key="ProxyElement" DataContext="{Binding}" />
            <wpf:BusyToCursorConverter x:Key="BusyToCursorConverter"/>
            <wpf:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <wpf:UpperCaseConvert x:Key="UpperCaseConvert"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="style/defaultStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <UserControl.Cursor>
        <Binding Path="DataContext.IsBusy" RelativeSource="{RelativeSource Self}" Converter="{StaticResource BusyToCursorConverter}"/>
    </UserControl.Cursor>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="60" />
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="95"/>
        </Grid.ColumnDefinitions>
        <Label Content="Reikia mokėti" Grid.Row="0" Style="{StaticResource boldLabel}"/>
        <Label Content="Mokama suma" Grid.Row="0" Grid.Column="1" Style="{StaticResource boldLabel}"/>
        <Label Content="Grąža" Grid.Row="0" Grid.Column="2" Style="{StaticResource boldLabel}"/>
        <TextBox Grid.Row="1" Grid.Column="0" Text="{Binding PaySum}" Style="{StaticResource defaultTextBox}" KeyDown="tb2Dcode_KeyDown" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DebtorSum}" Style="{StaticResource defaultTextBox}" KeyDown="tb2Dcode_KeyDown" />
        <TextBox Grid.Row="1" Grid.Column="2" Text="{Binding RestSum}" Style="{StaticResource defaultTextBox}" KeyDown="tb2Dcode_KeyDown" />
        <Button Content="KASA (F4)" Grid.Row="1" Grid.Column="3" Command="{Binding ParseCommand}" Style="{StaticResource defaultButton}"/>
        <Button Content="Banko kort. (F2)" Grid.Row="2" Grid.Column="0" Command="{Binding ManualInputCommand}" Style="{StaticResource defaultButton}"/>
        <Button Content="Grynais (F3)" Grid.Row="2" Grid.Column="1" Command="{Binding ManualInputCommand}" Style="{StaticResource defaultButton}"/>
        <ContentControl Content="{StaticResource ProxyElement}" Visibility="Collapsed" />
        <DataGrid x:Name="dataGrid" Grid.Row="3" Grid.ColumnSpan="6" ItemsSource="{Binding Path=fmd_models, Mode=TwoWay, NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged, IsAsync=True}" Style="{StaticResource defaultDataGrid}" SelectedItem="{Binding selected_fmd_model}" CellStyle="{StaticResource defaultDataGridCell}">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding productCode}" Header="Mokėjimo tipas" Width="SizeToCells" MinWidth="100" IsReadOnly="True"/>
                <DataGridTextColumn Binding="{Binding serialNumber}" Header="Suma" Width="SizeToCells" MinWidth="120" IsReadOnly="True"/>
                <DataGridTextColumn Binding="{Binding batchId}" Header="Kodas" Width="*" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
