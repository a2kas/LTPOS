<UserControl x:Class="POS_display.wpf.View.RecipeList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:POS_display.wpf.View"
             xmlns:wpf="clr-namespace:POS_display.wpf"
             xmlns:POS_display="clr-namespace:POS_display"
             mc:Ignorable="d" 
             d:DesignHeight="179" d:DesignWidth="571">
    <UserControl.Resources>
        <ResourceDictionary>
            <wpf:BusyToCursorConverter x:Key="BusyToCursorConverter"/>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../style/defaultStyle.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <UserControl.Cursor>
        <Binding Path="DataContext.IsBusy" RelativeSource="{RelativeSource Self}" Converter="{StaticResource BusyToCursorConverter}"/>
    </UserControl.Cursor>
    <Grid>
        <DataGrid x:Name="dataGrid">
            <DataGrid.Resources>
                <wpf:DateFormat x:Key="DateFormat"/>
                <wpf:TypeFormat x:Key="TypeFormat"/>
                <wpf:ERecipeGenericNameFormat x:Key="ERecipeGenericNameFormat"/>
                <Style TargetType="{x:Type DataGrid}" BasedOn="{StaticResource defaultDataGrid}">
                    <EventSetter Event="Selector.SelectionChanged" Handler="dataGrid_SelectionChanged" />
                    <Setter Property="ItemsSource" Value="{Binding RecipeList}"/>
                </Style>
                <Style TargetType="{x:Type DataGridCell}" BasedOn="{StaticResource {x:Type DataGridCell}}">
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self}, Path=Foreground}" />
                            <Setter Property="Background" Value="#FFC7BDBD" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <Setter Property="Height" Value="Auto"/>
                    <Setter Property="MinHeight" Value="25"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Status}" Value="active">
                            <Setter Property="Foreground" Value="Green"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding StatusReasonCode}" Value="reserved">
                            <Setter Property="Foreground" Value="Blue"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding StatusReasonCode}" Value="for review">
                            <Setter Property="Foreground" Value="Red"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="enteredinerror">
                            <Setter Property="Foreground" Value="Red"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Status}" Value="stopped">
                            <Setter Property="Foreground" Value="Red"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding RecipeNumber}" x:Name="RecipeNumber" Header="Recepto nr." MinWidth="{Binding Path=ActualWidth, ElementName=RecipeNumber }" />
                <DataGridTextColumn x:Name="GenericName2" Header="Pavadinimas" ElementStyle="{StaticResource WrapTextBlock}" Width="*">
                    <DataGridTextColumn.Binding>
                        <MultiBinding Converter="{StaticResource ERecipeGenericNameFormat}">
                            <Binding Path="Type"/>
                            <Binding Path="ProprietaryName"/>
                            <Binding Path="GenericName"/>
                        </MultiBinding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                <DataGridTextColumn Binding="{Binding Type, Converter={StaticResource TypeFormat}}" x:Name="Type" Header="Klasifikatorius" ElementStyle="{StaticResource WrapTextBlock}" MaxWidth="85" MinWidth="{Binding Path=ActualWidth, ElementName=Type }" />
                <DataGridTextColumn Binding="{Binding ValidFrom, Converter={StaticResource DateFormat}}" x:Name="ValidFrom" Header="Galioja nuo" MinWidth="{Binding Path=ActualWidth, ElementName=ValidFrom }" />
                <DataGridTextColumn Binding="{Binding ValidTo, Converter={StaticResource DateFormat}}" x:Name="ValidTo" Header="Galioja iki" MinWidth="{Binding Path=ActualWidth, ElementName=ValidTo }" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
